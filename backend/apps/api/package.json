{
  "name": "@tourdeal/api",
  "version": "0.0.1",
  "description": "TourDeal API Application",
  "scripts": {
    "build": "nest build api",
    "start": "nest start api",
    "start:dev": "nest start api --watch",
    "start:debug": "nest start api --debug --watch",
    "test": "jest --testPathPattern=apps/api",
    "test:watch": "jest --watch --testPathPattern=apps/api",
    "test:cov": "jest --coverage --testPathPattern=apps/api",
    "test:e2e": "jest --config ./test/jest-e2e.json"
  },
  "dependencies": {},
  "devDependencies": {}
}